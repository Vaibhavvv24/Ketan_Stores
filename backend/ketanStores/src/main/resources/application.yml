spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ketan_stores
    username: springstudent
    password: springstudent

    driver-class-name: com.mysql.cj.jdbc.Driver
  
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

  jpa:
    hibernate:
      ddl-auto: update
  security:
    user:
      password: spring
